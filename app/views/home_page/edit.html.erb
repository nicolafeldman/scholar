<%= form_for @home_page, :url => lol_path, :html => { :multipart => true } do |f| %>

<%= file_field_tag :photo, multiple: true %>

<%= f.submit "go go go", class: "btn btn-large btn-primary"%>

<% end  %>