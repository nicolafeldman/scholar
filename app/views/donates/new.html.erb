Update the photos and text now; you can add a brochure later. <br>

Choose TWO photos with portrait orientation: <br>

<%= form_for @donate, :html => { :class => 'form-horizontal', multipart: true } do |f| %>

      <%= file_field_tag "images[]", type: :file, multiple: true %>
      <br>

      <%= f.label :text, :class => 'control-label' %>
      <%= f.text_field :text, :class => 'text_field' %>


    <%= f.submit nil, :class => 'btn btn-primary' %>

<% end %>