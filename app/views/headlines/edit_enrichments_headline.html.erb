<h4>Update headline for Enrichment page:</h4>
    <%= form_for @headline, url: {controller: "headlines", action: "update_enrichments_headline"}, :html => { :multipart => true }, method: :patch do |form| %>

      <%= form.text_field :enrichments %><br><br>

      <%= form.submit "Update headline", class: "btn btn-large" %>
    <% end %>