<h4>Update headline for Apply page:</h4>
    <%= form_for @headline, url: {controller: "headlines", action: "update_apply_headline"}, :html => { :multipart => true }, method: :patch do |form| %>

      <%= form.text_field :apply %><br><br>

      <%= form.submit "Update headline", class: "btn btn-large" %>
    <% end %>