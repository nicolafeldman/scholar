<h4>Update headline for Media index page:</h4>
    <%= form_for @headline, url: {controller: "headlines", action: "update_media_headline"}, :html => { :multipart => true }, method: :patch do |form| %>

      <%= form.text_field :media %><br><br>

      <%= form.submit "Update headline", class: "btn btn-large" %>
    <% end %>