<h4>Update headline for Contact page:</h4>
    <%= form_for @headline, url: {controller: "headlines", action: "update_contact_headline"}, :html => { :multipart => true }, method: :patch do |form| %>

      <%= form.text_field :contact %><br><br>

      <%= form.submit "Update headline", class: "btn btn-large" %>
    <% end %>