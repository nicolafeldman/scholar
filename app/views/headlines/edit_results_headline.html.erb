<h4>Update headline for the 'Where are they now?' page:</h4>
    <%= form_for @headline, url: {controller: "headlines", action: "update_results_headline"}, :html => { :multipart => true }, method: :patch do |form| %>

      <%= form.text_field :results %><br><br>

      <%= form.submit "Update headline", class: "btn btn-large" %>
    <% end %>