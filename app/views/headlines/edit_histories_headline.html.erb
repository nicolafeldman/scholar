<h4>Update headline for History page:</h4>
    <%= form_for @headline, url: {controller: "headlines", action: "update_histories_headline"}, :html => { :multipart => true }, method: :patch do |form| %>

      <%= form.text_field :histories %><br><br>

      <%= form.submit "Update headline", class: "btn btn-large" %>
    <% end %>