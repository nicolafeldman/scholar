<h4>Update headline for the 'Study with SCHOLAR' page:</h4>
    <%= form_for @headline, url: {controller: "headlines", action: "update_studies_headline"}, :html => { :multipart => true }, method: :patch do |form| %>

      <%= form.text_field :studies %><br><br>

      <%= form.submit "Update headline", class: "btn btn-large" %>
    <% end %>