<h4>Update headline for FAQ page:</h4>
    <%= form_for @headline, url: {controller: "headlines", action: "update_faqs_headline"}, :html => { :multipart => true }, method: :patch do |form| %>

      <%= form.text_field :faqs %><br><br>

      <%= form.submit "Update headline", class: "btn btn-large" %>
    <% end %>