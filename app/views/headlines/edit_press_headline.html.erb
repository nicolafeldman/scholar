<h4>Update headline for Press page:</h4>
    <%= form_for @headline, url: {controller: "headlines", action: "update_press_headline"}, :html => { :multipart => true }, method: :patch do |form| %>

      <%= form.text_field :press %><br><br>

      <%= form.submit "Update headline", class: "btn btn-large" %>
    <% end %>