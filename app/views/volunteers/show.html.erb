<%= render 'layouts/photoscroll' %>

<div class="row-fluid">


<div class="text-container span9">

<p class="headline"><%= Headline.first.volunteers %></p>
<% if signed_in? %>
	<%= button_to "Edit headline", '/edit_volunteers_headline', :method => :get, class: "btn" %>
<% end %>

<% if !@volunteer.pictures.first.nil? %>
<div id="enrich-photo-right">
	<img id="enrich-photo" src=<%= @volunteer.pictures.first.image.url %> >
</div>

<div id="empty-div">&nbsp;</div>

<div id="enrich-photo-left">
	<img id="enrich-photo" src=<%= @volunteer.pictures.last.image.url %> >
</div>
<% end %>

<p><%= simple_format(h @volunteer.text) %></p>

<% if signed_in? %>
<%= button_to "Update this page", edit_volunteer_path(1), :method => :get, :form_class => "edit", class: "btn btn-large" %>
<% end %>
</div>




<%= render 'layouts/sidebar' %>

</div>
