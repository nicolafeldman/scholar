<div class="container">
	<ul>
      <% @medium.pictures.each do |picture| %>
		<div class="col-lg-3 col-sm-4 col-xs-6"><%= link_to image_tag(picture.image.url(:thumb)), picture.image.url, class: "thumbnail img-responsive", :rel => "lightbox" %></div>
	  <% end %>
	</ul>
</div>



